load("@//rule:process_json.bzl", "process_json")
load("@rules_pkg//pkg:mappings.bzl", "pkg_files")

process_json(
    name = "mcmeta_compress",
    srcs = ["pack.mcmeta"],
)

pkg_files(
    name = "mcmeta",
    srcs = [":mcmeta_compress"],
    visibility = ["//visibility:public"],
)
